bin/
tmp/
dist/
.superpowers/
coverage.out
coverage.html
coverage-*.out
coverage-*.html

.env
.env.local
.env.*.local
# Env backups with real values (only .env.example templates are committed)
.env.*-backup

serviceAccountKey.json
serviceAccountKey*.json
*.pem
*.key

# Firebird seeds with real values (only .example templates are committed)
seeds-firebird/*.sql
!seeds-firebird/*.sql.example

# Firebird DB snapshots (created by `make fb-snapshot`; large binary, local-only)
data/firebird-snapshots/
*.fbk

uploads/
private_files/

.DS_Store
.idea/
.vscode/
*.swp

node_modules/

# Compiled local binary built from cmd/seed-cobrador (build artifact, not shipped)
/seed-cobrador

# rapid (property-testing) failure reproductions — regenerated on demand, not committed
**/testdata/rapid/
