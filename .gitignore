bin/
tmp/
dist/
coverage.out
coverage.html
coverage-*.out
coverage-*.html

.env
.env.local
.env.*.local

serviceAccountKey.json
*.pem
*.key

# Firebird seeds with real values (only .example templates are committed)
seeds-firebird/*.sql
!seeds-firebird/*.sql.example

# Firebird DB snapshots (created by `make fb-snapshot`; large binary, local-only)
data/firebird-snapshots/
*.fbk

uploads/
private_files/

.DS_Store
.idea/
.vscode/
*.swp

node_modules/
