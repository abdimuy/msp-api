# Offline credit-scorecard fitting harness (R4) — dev-only
# Run from repo root: pip install -r analysis/creditscorecard/requirements.txt
#
# Pin compatible, recent versions.  Update after verifying the full pipeline
# produces consistent scorecard.json output.

firebird-driver>=1.10.0,<2.0
scikit-learn>=1.4.0,<2.0
pandas>=2.2.0,<3.0
numpy>=1.26.0,<3.0
