// Package domain holds the comprobantes module's aggregate root, child
// entities, value objects, domain events, and sentinel errors. It depends
// only on the standard library and the platform/{domain,apperror,audit}
// packages — never on app, infra, ports, or other modules.
//
//nolint:misspell // domain vocabulary is Spanish (envío, comprobante, etc.) per project convention.
package domain
